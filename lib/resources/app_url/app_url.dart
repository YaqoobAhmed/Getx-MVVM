class AppUrl {
  static const String baseUrl =
      'https://reqres.in'; //because base url will be same all the time and if server changes we just need to chanje the base url

  static const String loginApi = '$baseUrl/api/login';
}
